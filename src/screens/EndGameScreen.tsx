import React from 'react';
import { Center, Heading } from 'native-base';

const EndGameScreen: React.FC = () => {
  return (
    <Center flex={1}>
      <Heading>EndGameScreen</Heading>
    </Center>
  );
};

export default EndGameScreen;
